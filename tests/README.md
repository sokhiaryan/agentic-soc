# tests

Controlled validation material for this lab.

- `atomic/` contains PowerShell helpers that preview an Atomic Red Team test by default and require explicit approval to execute it.
- `scenarios/` is reserved for short, documented Wazuh test scenarios.
- `expected-results/` can hold small, non-sensitive expected alert examples.
