# architecture/

Final, rendered architecture diagrams referenced from the root README: `system.drawio` / `system.png`, `investigation-flow.png`, `response-flow.png`.

Working versions and the accompanying explanation live in `docs/architecture/`; this folder holds the polished exports.
