# docs/ai/

Design and evaluation notes for the AI analyst and agentic investigator: schema design rationale (`schemas/`), prompt iteration history (`prompts/`), tool design for the agentic investigator, and accuracy/calibration evaluation against `tests/scenarios/`.
