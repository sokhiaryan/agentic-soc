# docs/n8n/

Design notes for the n8n SOAR layer: workflow diagrams, node-level explanations, and documented failures (e.g. auth/token propagation issues between HTTP Request nodes). Exported workflow JSON lives in the top-level `workflows/`.
