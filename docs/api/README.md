# docs/api/

`wazuh-api-notes.md` — one entry per Wazuh REST API endpoint used in this project: endpoint, method, purpose, authentication, parameters, request, response, important fields, failure modes, security considerations, and the n8n equivalent. Built up endpoint-by-endpoint alongside the Postman collection in `postman/wazuh-api/`.
