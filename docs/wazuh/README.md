# docs/wazuh/

Deep dives specific to Wazuh internals. First planned document: `event-anatomy.md` — a real Wazuh alert JSON, annotated field by field (timestamp, agent block, rule block, data block), written after generating and inspecting real telemetry (Stage 3), not from documentation alone.
