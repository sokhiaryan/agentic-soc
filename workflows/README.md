# workflows/

Exported n8n workflow JSON (`alert-ingestion.json`, `enrichment.json`, `investigation.json`, `response.json`), each importable directly into n8n. Design rationale and screenshots live in `docs/n8n/`.
